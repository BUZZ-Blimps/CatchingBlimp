piStereoVision program takes in the blimp ID as a parameter.
To configure the pi's autostart:
- ssh into pi
- cd /etc
- sudo nano rc.local
- edit autostart configuration (add blimp ID as a parameter to the program execution call)
